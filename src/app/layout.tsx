import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "GerenteMax - Login",
  description: "Sistema de gestão empresarial GerenteMax - Faça login para acessar sua conta",
  keywords: ["gestão", "empresarial", "login", "gerentemax"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}